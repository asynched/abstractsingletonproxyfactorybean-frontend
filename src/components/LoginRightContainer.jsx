export default function LoginRightContainer({ children }) {
  return (
    <div className="col-span-3 hidden w-full h-full lg:flex items-center justify-center bg-gradient-to-r from-purple-500 to-indigo-500">
      {children}
    </div>
  )
}
